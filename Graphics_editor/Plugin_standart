struct Obj
{
	// указатели на функции	
};

struct Renderer
{
	int width, height;
	uint32_t *
	// указатели на функции, которые заполняются главной программой	
	draw_circle
	draw_rectangle
	draw_line
	draw_point
};

// функции

apply()
